(Nội dung ngày 13/6/2026)
Đã sửa file subscribe_addLPR_Face.py, sketch_aug17a_mqtt.ino (file code arduino main wemos)
# 🚗 Hệ Thống Quản Lý Bãi Xe Thông Minh (Smart Parking System)

Hệ thống quản lý bãi xe thông minh tích hợp toàn diện giữa thiết bị phần cứng điều khiển nhúng và máy chủ xử lý trung tâm ứng dụng trí tuệ nhân tạo (AI). Hệ thống vận hành theo cơ chế **Hybrid (MQTT + HTTP)**, hỗ trợ nhận diện biển số (LPR), nhận diện khuôn mặt (Face Recognition), kiểm soát luồng xe ra vào tự động và đồng bộ hóa tài chính thời gian thực.

---

## 🏗️ Kiến Trúc Hệ Thống & Nguyên Lý Vận Hành

Hệ thống được chia làm 2 thành phần cốt lõi hoạt động song song và bổ trợ lẫn nhau:

### 1. Phân Hệ Thiết Bị Đầu Cuối (Hardware - Node Edge)
* **Vi điều khiển:** Wemos D1 R2 (ESP8266) cấu hình IP tĩnh nhằm tối ưu tốc độ và độ ổn định kết nối.
* **Cơ chế truyền thông:** Vận hành song song hai giao thức:
  * **MQTT (Giao thức chính):** Truyền nhận dữ liệu thời gian thực (Publish trạng thái ô đỗ, trạng thái cảm biến hình ảnh, sự kiện quẹt thẻ; Subscribe lệnh điều khiển cổng từ Server).
  * **HTTP Server (Giao thức dự phòng/Đồng bộ):** Khởi tạo một Web Server mini nội bộ tại địa chỉ `http://172.16.10.172/status`. Khi Server Python mất kết nối và trực tuyến trở lại, nó sẽ chủ động quét HTTP để đồng bộ hóa trạng thái ô đỗ ngay lập tức mà không cần đợi sự kiện từ MQTT.
* **Quản lý ngoại vi:** Giám sát 2 ô đỗ (`A1`, `B1`), 2 cảm biến hồng ngoại (IR) chắn cổng, và điều khiển 2 động cơ Servo đóng/mở thanh chắn barrier.

### 2. Phân Hệ Máy Chủ Trung Tâm (Backend Server - Python)
* **Xử lý đa luồng (Multi-threading):** Tách biệt luồng lắng nghe MQTT, luồng Web Stream (Flask), và các luồng xử lý AI (Worker) nhằm đảm bảo hệ thống không bị nghẽn (Non-blocking).
* **Trí tuệ nhân tạo (AI):** Tích hợp module quét biển số (`plate_scanner`) và module kiểm tra khuôn mặt (`face_detect_deepface_faster`) để thực hiện xác thực hai lớp (Biển số xe + Gương mặt tài xế).
* **Hệ quản trị CSDL:** MySQL (`pymysql`) lưu trữ lịch sử đỗ xe, trạng thái chuồng và thông tin hóa đơn.

---

## 📊 Bản Đồ Hàm & Logic Code Chi Tiết

### 1. Phân Hệ Phần Cứng (`sketch_aug17a_mqtt.ino`)

| Tên Hàm | Tác Dụng / Nhiệm Vụ Cốt Lõi | Logic Vận Hành |
| :--- | :--- | :--- |
| `setup()` / `loop()` | Khởi tạo & Duy trì vòng lặp | Thiết lập LCD, Servo, WiFi, HTTP Server, MQTT Client. Duy trì kiểm tra kết nối và cập nhật trạng thái ngoại vi liên tục sau mỗi 20ms. |
| `connectWiFi()` / `checkConnection()` | Quản lý kết nối mạng | Thiết lập IP tĩnh. Tự động kiểm tra trạng thái kết nối sau mỗi 4 giây (`CONN_CHECK_INTERVAL`). Nếu mất kết nối MQTT, tự chuyển sang chế độ `OFFLINE` để chạy độc lập. |
| `mqttCallback()` | Lắng nghe lệnh từ Server | Tiếp nhận các gói tin từ topic `parking/gate/cmd`, bật các cờ hiệu điều khiển tương ứng (`webRequestOpenEntry`, `webRequestCloseEntry`, v.v.). |
| `handleHttpStatus()` | Cung cấp API Realtime | Khi nhận request HTTP GET, tự động đóng gói trạng thái 2 ô đỗ xe dưới dạng JSON (`{"A1": "O/X", "B1": "O/X"}`) và trả về với HTTP Status 200 OK. |
| `checkRFID()` | Đọc & Xác thực thẻ từ xa | Đọc dữ liệu từ Serial, chuẩn hóa UID (chuyển chữ hoa, xóa khoảng trắng). So khớp với danh sách `allowedUIDs` (Whitelist Offline). Đẩy dữ liệu log quẹt thẻ lên MQTT. |
| `statusGateIR()` | Giám sát vật cản tại cổng | Đọc cảm biến hồng ngoại tại cổng Vào/Ra. Phát hiện sự thay đổi trạng thái và gửi tín hiệu (`O`: Có xe chắn, `X`: Trống) lên MQTT. |
| `handleSlots()` | Quản lý trạng thái chuồng | Đọc cảm biến tại 2 ô đỗ xe. Áp dụng bộ lọc chống nhiễu **Debounce** (2 giây). Nếu trạng thái thay đổi ổn định, kích hoạt các hàm phát tín hiệu và cập nhật LCD. |
| `publishSlotChange()` / `publishSlotsCount()` | Đồng bộ trạng thái chuồng | Gửi trạng thái chi tiết của từng ô và tổng số chuồng trống lên các topic MQTT (`parking/slot/...`, `parking/slots/count`) và đẩy dữ liệu dạng JSON vào `parking/log`. |
| `countFreeSlots()` | Tính toán dung lượng bãi | Duy trì vòng lặp đếm số lượng ô đỗ có trạng thái bằng trống (`false`) và trả về số nguyên. |
| `openEntryGate()` / `closeEntryGate()` <br> `openExitGate()` / `closeExitGate()` | Điều khiển Barrier vật lý | Ghi góc quay cho Servo (`OPEN_ANGLE = 180`, `CLOSE_ANGLE = 0`). Hàm mở cổng vào tích hợp điều kiện chặn: Nếu không phải lệnh từ MQTT và bãi đã đầy (`countFreeSlots() == 0`), sẽ từ chối mở cổng. |
| `updateLCD()` | Hiển thị thông tin lên màn hình | Kiểm tra chuỗi ký tự cũ và mới (`lastLCDLine`). Nếu có thay đổi mới thực hiện ghi đè nhằm **chống nhấp nháy màn hình (Flicker)**. Hiển thị trạng thái ô đỗ và chế độ kết nối (`NORM` / `NO_MQTT`). |

### 2. Phân Hệ Máy Chủ (`subscribe_addLPR_Face.py`)

| Tên Hàm | Tác Dụng / Nhiệm Vụ Cốt Lõi | Logic Vận Hành |
| :--- | :--- | :--- |
| `get_db_connection()` | Quản lý kết nối MySQL | Sử dụng cơ chế Auto-Reconnect qua lệnh `.ping(reconnect=True)`, ngăn ngừa lỗi mất kết nối DB khi hệ thống chạy trong thời gian dài. |
| `save_cache()` / `load_cache()` <br> `clear_cache()` | Cơ chế Khôi phục Sự cố <br> (**Crash Recovery**) | Ghi/Đọc dữ liệu ngữ cảnh hiện tại (xe đang chờ lối ra, thẻ vừa quẹt lối vào) vào bảng `system_cache`. Giúp hệ thống khôi phục hoàn hảo trạng thái làm việc nếu server bị sập và khởi động lại đột ngột. |
| `write_log()` | Ghi nhận nhật ký cổng | Ghi chép mọi hành động đóng/mở/từ chối của cổng vào bảng `gatelog` phục vụ công tác giám sát, hậu kiểm của người quản trị. |
| `entry_worker()` | Tiến trình xử lý xe VÀO | Chạy trên một Thread riêng: Chụp ảnh quét biển số (LPR) $
ightarrow$ Chụp ảnh nhận diện khuôn mặt $
ightarrow$ Lưu thông tin vào `parkinghistory` $
ightarrow$ Phát lệnh mở cổng qua MQTT $
ightarrow$ Lưu trạng thái vào bộ nhớ đệm `pendingEntry`. |
| `exit_worker()` | Tiến trình xử lý xe RA | Chạy trên một Thread riêng: Quét biển số lối ra $
ightarrow$ So khớp với biển số lúc vào $
ightarrow$ So khớp khuôn mặt tài xế với lúc vào $
ightarrow$ Tính toán số phút, quy đổi số giờ đỗ xe $
ightarrow$ Tạo hóa đơn trong bảng `payments`. |
| `check_paid_and_open()` | Quét và duyệt thanh toán | Vòng lặp chạy ngầm liên tục quét CSDL. Khi phát hiện hóa đơn của xe đang chờ ở cổng ra đã được thanh toán (`Status='paid'` và `Notified=0`), tự động cập nhật số tiền vào `parkinghistory`, phát lệnh mở cổng `OPEN_EXIT` và đánh dấu `Notified=1`. |
| `on_message()` | Bộ điều hướng sự kiện MQTT | Trung tâm tiếp nhận và phân phối dữ liệu từ MQTT Broker: Cập nhật trạng thái cảm biến IR; Xử lý sự kiện thay đổi ô đỗ (`slot_change`); Điều hướng luồng thẻ RFID hợp lệ sang `entry_worker` hoặc `exit_worker`. |
| `admin_force_checkout()` | API hỗ trợ Quản trị viên | Định tuyến Flask `/admin/force-checkout/<id>`. Cho phép Admin cưỡng bức chốt thời gian ra, tính phí, giải phóng chuồng và xóa trạng thái kẹt thẻ của các xe gặp sự cố bằng tay thông qua Web Admin. |
| `sync_hardware_realtime()` | Đồng bộ hóa khi khởi động | Gửi một HTTP request `GET` trực tiếp tới Wemos. Lấy dữ liệu trạng thái thực tế của các ô đỗ để cập nhật ngược lại CSDL và bộ nhớ RAM của Python ngay khi script vừa khởi chạy. |

---

## 🌟 Điểm Mạnh & Các Giải Pháp Công Nghệ Đột Phá

Hệ thống được thiết kế và tối ưu hóa sâu để giải quyết triệt để các bài toán thực tế trong vận hành nhà xe thông minh:

* **⚡ Cơ chế Hồi sinh sau sự cố (Crash Recovery):** Đây là điểm sáng thiết kế lớn nhất. Bằng cách lưu ngữ cảnh giao dịch vào `system_cache`, nếu Server Python bị sập nguồn đột ngột, ngay khi bật lại, hệ thống tự động nhận diện được xe nào đang đợi ở lối RA, xe nào vừa quẹt lối VÀO để tiếp tục xử lý, tránh việc bắt tài xế phải lùi xe quẹt lại thẻ từ đầu.
* **🌐 Kiến trúc kết hợp Hybrid (MQTT + HTTP):** Tận dụng tính chất real-time, tốn ít băng thông của MQTT cho các tác vụ truyền tin liên tục. Tuy nhiên, tích hợp thêm HTTP Server nội bộ trên Wemos giúp giải quyết bài toán đồng bộ ban đầu: Khi Server Python Onl trở lại, nó chỉ cần gọi 1 request HTTP duy nhất để đồng bộ toàn bộ trạng thái bãi xe.
* **🔒 Chốt đóng lịch sử bằng Cảm biến Vật lý an toàn:** Hệ thống không tự ý chốt thời gian ra (`TimeOut`) và giải phóng chuồng khi barrier vừa mở. Trạng thái chỉ được ghi nhận hoàn tất khi cảm biến hồng ngoại lối ra ghi nhận chu trình đổi trạng thái từ `O` (Xe đang đi qua barrier) sang `X` (Xe đã đi qua hẳn), đảm bảo tính toàn vẹn dữ liệu, tránh thất thoát doanh thu.
* **🛡️ Chống xung đột tài nguyên Camera (Thread-safe):** Các tiến trình gọi Camera/AI được đồng bộ hóa thông qua `camera_lock = threading.Lock()`. Điều này đảm bảo khi có 2 xe cùng quẹt thẻ vào và ra tại một thời điểm, các luồng ảnh sẽ được xếp hàng xử lý tuần tự, tránh xung đột phần cứng gây treo ứng dụng.
* **📉 Chống nhiễu Cảm biến thông minh (Sensor Debounce):** Tại các ô đỗ, khi xe đang lùi vào, cảm biến siêu âm/hồng ngoại rất dễ bị nhảy trạng thái liên tục. Việc cấu hình bộ lọc chống nhiễu `DEBOUNCE_DELAY_MS = 2000` trên Wemos giúp lọc bỏ hoàn toàn tín hiệu nhiễu, tránh spam hàng ngàn tin nhắn rác lên MQTT Broker.
* **🧩 Tách biệt hóa đơn và Luồng điều khiển:** Tách rời quá trình quẹt thẻ tạo hóa đơn (`exit_worker`) và tiến trình quét duyệt mở cổng (`check_paid_and_open`). Mô hình này cho phép bãi xe dễ dàng tích hợp các hệ thống thanh toán tự động (Mã QR tĩnh/động, Máy POS), cổng chỉ mở khi hệ thống ngân hàng/POS báo đã thu tiền thành công vào DB.
* **🧹 Giao diện Console sạch sẽ:** Hệ thống sử dụng bộ lọc ẩn các log Heartbeat (`parking/system/heartbeat`) và tắt cảnh báo thông tin của thư viện Flask (`werkzeug`), giúp màn hình quản trị của Server chỉ hiển thị các log nghiệp vụ quan trọng, dễ dàng theo dõi và xử lý khi có sự cố.



Đã thêm file session_start.php thay cho session_start(); thuần
Sửa file control.php, mqtt_control.php, checkUser.php
* **🛡️ Phòng chống tấn công CSRF (Cross-Site Request Forgery):** Hệ thống được cấu hình bảo mật nghiêm ngặt nhằm ngăn chặn các kịch bản kẻ gian lợi dụng phiên đăng nhập (Session) của Admin để gửi lệnh giả mạo điều khiển barrier hoặc sửa đổi cơ sở dữ liệu:
  * **Cấu hình thuộc tính SameSite cho Cookie:** Các Cookie định danh và phiên làm việc được thiết lập cờ `SameSite=Strict` (hoặc `Lax` tùy theo môi trường triển khai). Cơ chế này ngăn chặn trình duyệt tự động gửi kèm Cookie bảo mật khi có các request xuất phát từ một trang web bên thứ ba (Cross-site), triệt tiêu hoàn toàn con đường tấn công cookie-lấy-gốc của CSRF.
  * **Tích hợp Token chống CSRF (CSRF Token):** Mọi yêu cầu thay đổi trạng thái (POST/PUT/DELETE) thông qua giao diện Web Admin hoặc các API can thiệp hệ thống (như tính năng `admin_force_checkout`) đều bắt buộc phải đính kèm một chuỗi mã ngẫu nhiên, duy nhất và có thời hạn (CSRF Token). Server Python (Flask) sẽ so khớp Token được gửi lên từ Request Header/Form với Token hợp lệ trong Session; nếu không khớp, hệ thống lập tức từ chối thực thi (HTTP 403 Forbidden).